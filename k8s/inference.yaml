apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: iris-model
  namespace: default
spec:
  predictor:
    containers:
      - name: iris-container
        image: kinglast/iris-model:v19
        ports:
          - containerPort: 8080